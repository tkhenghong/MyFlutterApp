class Question {
  final String question; //Set them final to prevent they will get changed when set once in the program.
  final bool answer;

  Question(this.question, this.answer); // Auto set question and answer variables when created.
}